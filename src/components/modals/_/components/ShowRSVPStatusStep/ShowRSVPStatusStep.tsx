import { ModalBody, Stack } from "@chakra-ui/react";

const ShowRSVPStatusStep = () => {
  return (
    <ModalBody gap={{ base: 4, lg: 5 }} minW="100%" as={Stack}>
      here
    </ModalBody>
  );
};

export default ShowRSVPStatusStep;
